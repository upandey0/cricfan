const SUCCESS = true;
const FAILURE = false;
const HEADER_SESSION_ID = "session-id";
const SESSION_ID_TTL = 180 * 24 * 60;

module.exports = {
    SUCCESS,
    FAILURE,
    HEADER_SESSION_ID,
    SESSION_ID_TTL
};